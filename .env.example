DATABASE_URL=postgresql://engageninja:engageninja@localhost:5433/engageninja
JWT_SECRET=changeme-access
JWT_REFRESH_SECRET=changeme-refresh
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d
API_PORT=3000
NEXT_PUBLIC_API_URL=http://localhost:3000

# Plan tier limits are now managed in the database via PlanTier table
# See packages/prisma/seed.js for plan tier seeding

# CRON jobs
QUOTA_RESET_CRON_ENABLED=true
