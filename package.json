{
  "name": "engageninja-monorepo",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "prepare-db": "pnpm --filter @engageninja/prisma prepare-db"
  },
  "dependencies": {
    "@nestjs/schedule": "^6.1.0",
    "@prisma/adapter-pg": "7.1.0",
    "date-fns": "^4.1.0"
  }
}
