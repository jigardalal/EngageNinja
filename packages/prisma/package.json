{
  "name": "@engageninja/prisma",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "db:push": "prisma db push",
    "seed": "prisma db seed",
    "prepare-db": "pnpm db:push && pnpm seed",
    "format": "prisma format"
  },
  "dependencies": {
    "@prisma/client": "7.1.0",
    "dotenv": "^16.6.1"
  },
  "devDependencies": {
    "prisma": "7.1.0"
  },
  "prisma": {
    "seed": "node ./seed.js"
  }
}
